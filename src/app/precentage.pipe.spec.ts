import { PrecentagePipe } from './precentage.pipe';

describe('PrecentagePipe', () => {
  it('create an instance', () => {
    const pipe = new PrecentagePipe();
    expect(pipe).toBeTruthy();
  });
});
